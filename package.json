{
  "name": "thetube-mcp",
  "version": "0.4.0",
  "description": "MCP proxy — thin layer over WebDAV + SQLite state",
  "type": "module",
  "main": "src/proxy.js",
  "scripts": {
    "start": "node src/proxy.js",
    "webdav": "node webdav/server.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1053.0",
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
